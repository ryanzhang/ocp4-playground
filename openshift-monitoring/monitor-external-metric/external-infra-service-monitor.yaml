apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: external-infra-monitor
  namespace: external-service
spec:
  endpoints:
  - interval: 30s
    port: web
    scheme: http
  selector:
    matchLabels:
      external-infra-monitor: "true"
