apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "amq-streams.fullname" . }}-test-connection"
  labels:
    {{- include "amq-streams.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "amq-streams.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
