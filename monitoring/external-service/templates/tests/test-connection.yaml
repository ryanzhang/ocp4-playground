apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prometheus-external-service.fullname" . }}-test-connection"
  labels:
    {{- include "prometheus-external-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prometheus-external-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
