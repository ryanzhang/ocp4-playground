修改my-grafana CR更改为 如下设置:
1. 允许动态绑定serviceaccount token 从命令行中
helm upgrade --install my-grafana my-grafana/ -n my-grafana --set token=$(oc serviceaccounts get-token grafana-serviceaccount -n my-grafana)
