apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-grafana.fullname" . }}-test-connection"
  labels:
    {{- include "my-grafana.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-grafana.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
